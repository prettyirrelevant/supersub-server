export const fetchSmartAccounts = () => {
  // first retrieve all privy eoa addresses.
  // check the database to see if those addresses are already present to avoid needless rpc calls
  // using the account factory, retrieve the smart account addresses.
};
